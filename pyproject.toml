[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aeon-termux-agent"
version = "0.1.1"
description = "Safe standard-library agent for Termux and remote GPU inference"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["aeon_agent", "tools"]

[tool.pytest.ini_options]
pythonpath = ["."]
